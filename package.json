{
  "devDependencies": {
    "@mermaid-js/mermaid-cli": "^11.16.0"
  },
  "name": "domesim-docs",
  "private": true,
  "version": "1.0.0",
  "description": "Diagram tooling for the teaching-video engine. Python does the rendering; this is only for docs.",
  "scripts": {
    "diagrams": "py -3.12 render_diagrams.py",
    "diagrams:png": "py -3.12 render_diagrams.py --format png --background #0a1420"
  }
}
